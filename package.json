{
  "name": "registry-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Registry parity suite (ADR 021) — authored against the Angular app, run against both apps.",
  "scripts": {
    "test:angular": "playwright test --project=angular",
    "test:nuxt": "playwright test --project=nuxt",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/node": "^24.0.0",
    "typescript": "^5.6.0"
  },
  "packageManager": "pnpm@11.17.0"
}
